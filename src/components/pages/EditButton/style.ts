import styled from "styled-components";

const Style = styled.div`
    button{
        padding: 0 !important;    
        min-width: 33px;
        .edit_icon{
            font-size: 1.3rem;
        }
    }
`

export default Style;